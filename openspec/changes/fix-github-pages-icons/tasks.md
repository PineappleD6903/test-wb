## 1. Update Asset Paths

- [x] 1.1 Update `src/utils/itemIcons.js` to prepend `import.meta.env.BASE_URL` to the returned URL in `getWeaponIcon`.
- [x] 1.2 Update `src/utils/itemIcons.js` to prepend `import.meta.env.BASE_URL` to the returned URL in `getArmorIcon`.
- [x] 1.3 Update `src/utils/itemIcons.js` to prepend `import.meta.env.BASE_URL` to the returned URL in `getItemIcon` (fallback logic).
