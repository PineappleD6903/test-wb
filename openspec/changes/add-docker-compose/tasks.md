## 1. Create Docker Compose File

- [x] 1.1 Create `docker-compose.yml` in the repository root.
- [x] 1.2 Define the `app` service with build context `.` and port mapping `5173:5173`.

## 2. Verification

- [x] 2.1 Verify that `docker compose up` correctly builds and starts the application, and the app is accessible on localhost:5173.
